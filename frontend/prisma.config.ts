/**
 * Prisma Configuration (Prisma 7 compatible)
 * Replaces deprecated package.json#prisma configuration
 */

export default {
  seed: 'tsx prisma/seed.ts',
  // Additional Prisma configuration can be added here as needed
}