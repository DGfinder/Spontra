# NPM Configuration for Spontra Frontend
# 
# legacy-peer-deps: Fallback for peer dependency conflicts
# Only needed if @react-email/components upgrade still causes issues
legacy-peer-deps=true

# Automatically use exact versions for security
save-exact=false

# Use the npm registry
registry=https://registry.npmjs.org/

# Production optimization
fund=false
audit=false